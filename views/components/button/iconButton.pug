mixin iconButton(classId, icon, label=null, attrs={})
    button.icon-button(class=classId)&attributes(attrs)
        .icon-button__icon
            img(src=icon)
        if label !== null
            .icon-button__label= label