mixin iconButton(classId, icon, label=null)
    button.icon-button(class=classId)&attributes(attributes)
        .icon-button__icon
            img(src=icon)
        if label !== null
            .icon-button__label= label