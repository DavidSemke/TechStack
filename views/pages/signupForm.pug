extends ../layouts/logging
include ../components/form/loggingForm

block loggingForm
    - const {inputs, errors} = data

    h1.logging-page__heading Sign Up
    +loggingForm('/signup', inputs, errors)