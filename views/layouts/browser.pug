extends toolbars
include ../components/icon/iconButton 
include ../components/icon/iconLink

block navbar
    .navbar__left
        +iconButton(
            '/images/burgerIcon.png'
        )(class='-burger' id='navbar-burger-button')
        +iconLink('/images/favicon.png', '/', 'Tech Stack')
    .navbar__center 
        include ../components/form/searchbar
    .navbar__right 
        if user
            +iconLink(
                '/images/writeIcon.png', 
                `/users/${user.username}/blogs/new-blog`,
                'Write'
            )(class='-button')
        else 
            a(href='/login' class='-button') Login
        
        +iconButton(
            '/images/profileIcon.png'
        )(class='-circle' id='navbar-account-button')

block sidebars
    include ../components/toolbar/contentSidebar
    include ../components/toolbar/accountSidebar
    block page